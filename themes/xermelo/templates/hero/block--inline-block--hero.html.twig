{% set classes = [
  'hero'
] %}

<section{{ attributes.addClass(classes) }}>
    {{ title_prefix }}
    {{ title_suffix }}

    <div class="container">
        <div class="grid">
            <div class="content">
                {{ content }}
            </div>
        </div>
    </div>

</section>
